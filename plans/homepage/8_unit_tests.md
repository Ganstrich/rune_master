# Plan 8: Unit tests for route

- Create or update test files (e.g., `tests/test_homepage.py`).
- Write tests to verify the route returns 200.
- Test valid form submission.
- Test invalid form submission (e.g., `min > max`, missing professions).
