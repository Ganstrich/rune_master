# Plan 2: Create HTML template

- Create `templates/home.html`.
- Add basic HTML5 structure.
- Add `<form>` with inputs for `min_level` and `max_level`.
- Add a container for dynamic profession inputs.
- Add "Add Profession" button.
- Add "Submit" button.
