# Plan 5: Validate form data (client-side)

- Update `static/js/home.js`.
- Add an event listener to the form's `submit` event.
- Validate that `min_level` and `max_level` are integers and `min <= max`.
- Validate that there is at least one profession entered.
- Display error messages in the UI instead of submitting if invalid.
